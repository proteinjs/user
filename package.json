{
  "name": "root",
  "private": true,
  "scripts": {
    "clean-workspace": "workspace clean",
    "build-workspace": "build-workspace --skip=@proteinjs/user-server,@proteinjs/user-ui",
    "test-workspace": "workspace test --skip=@proteinjs/user-server,@proteinjs/user-ui",
    "watch-workspace": "watch-workspace"
  },
  "devDependencies": {
    "@proteinjs/build": "^1.0.15",
    "lerna": "8.1.2"
  }
}
